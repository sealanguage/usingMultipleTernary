function checkSign(num) {
    
    return (num > 0) ? "positive" : (num < 0) ? "negative" : "zero";
  
}

checkSign(10);

// 0 returns zero
// positive numbers return positive
// negative numbers return negative